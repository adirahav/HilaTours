{
  "name": "hila-tours",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "user-management-service",
    "tour-service"
  ],
  "scripts": {
    "dev": "concurrently -n tour,user -c blue,green,yellow,magenta,cyan \"npm:dev --prefix common-service\" \"npm:dev --prefix analytics-service\" \"npm:dev --prefix tour-service\" \"npm:dev --prefix user-management-service\"",
    "start": "concurrently -n gateway,tour,user -c white,blue,green,yellow,magenta,cyan \"node --env-file=.env.production server.js\" \"npm:start --prefix tour-service\" \"npm:start --prefix user-management-service\""
  },
  "dependencies": {
    "concurrently": "^9.2.4",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "http-proxy-middleware": "^3.0.3"
  }
}
